import styled from 'styled-components';

const StyledAvatar = styled.div`
  width: 40px;
  height: 40px;
  border-radius: 100%;
  overflow: hidden;
`;

const StyledAvatarImage = styled.img`
  display: block;
  max-width: 100%;
`;

export {
  StyledAvatar,
  StyledAvatarImage,
};
